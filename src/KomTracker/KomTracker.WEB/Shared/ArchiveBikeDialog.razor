@using System.Net.Http.Json
@using KomTracker.API.Shared.ViewModels.Bike
@using KomTracker.Domain.Entities.Bike
@using KomTracker.WEB.Infrastructure

<MudDialog>
    <DialogContent>
        <MudText Typo="Typo.body2" Class="mb-3">Archive <b>@Bike.Name</b> — it stays yours but drops out of the active garage.</MudText>
        <MudForm @ref="_form">
            <MudTextField @bind-Value="_notes" Label="Notes" Lines="3" MaxLength="2000" />
        </MudForm>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">Cancel</MudButton>
        <MudButton Color="Color.Primary" Variant="Variant.Filled" OnClick="SubmitAsync" Disabled="_saving">Archive</MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter]
    private IMudDialogInstance MudDialog { get; set; } = default!;

    [Parameter] public BikeViewModel Bike { get; set; } = default!;

    [Inject] private HttpClient Http { get; set; } = default!;
    [Inject] private ISnackbar Snackbar { get; set; } = default!;

    private MudForm _form = default!;
    private string? _notes;
    private bool _saving;

    protected override void OnInitialized() => _notes = Bike.Notes;

    private async Task SubmitAsync()
    {
        _saving = true;

        try
        {
            var body = new ChangeBikeLifecycleViewModel
            {
                Lifecycle = BikeLifecycle.Archived,
                Notes = _notes ?? string.Empty
            };

            var response = await Http.PutAsJsonAsync($"bikes/{Bike.Id}/lifecycle", body);

            if (response.IsSuccessStatusCode)
            {
                Snackbar.Add("Bike archived", Severity.Success);
                MudDialog.Close(DialogResult.Ok(true));
                return;
            }

            await response.ShowProblemAsync(Snackbar);
        }
        finally
        {
            _saving = false;
        }
    }

    private void Cancel() => MudDialog.Cancel();
}
